'use client';
import React from 'react';
import { motion } from 'framer-motion';
import Table from './_components/Table';

const page = () => {
  return (
    <div className="py-4">
      <Table />
    </div>
  );
};

export default page;
